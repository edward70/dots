# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=skylake -O2 -pipe"
MAKEOPTS="-j4"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# accept all licenses
ACCEPT_LICENSE="*"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

GENTOO_MIRRORS="https://mirror.aarnet.edu.au/pub/gentoo/"

# more additions
USE="X elogind xinerama networkmanager xft alsa opengl png -gpm -afs -audit -bluetooth -modemmanager -ofono -ppp -w3m -initramfs -apm -netifrc -amdgpu -nouveau -radeon -tegra -via -virtualbox -vmware -nvidia -radeonsi -pulseaudio -systemd -aqua -css -a52 -cjk -gnome -ldap -3df -clamav -coreaudio -ios -ipod -ieee1394 -emacs -xemacs -gtk -motif -emboss -3dfx -altivec -smartcard -ibm -nls -nas -neon -nntp -cups -quicktime -wayland"
VIDEO_CARDS="intel i965"
INPUT_DEVICES="libinput"
LC_MESSAGES=C
FEATURES="collision-protect parallel-fetch parallel-install sign"
